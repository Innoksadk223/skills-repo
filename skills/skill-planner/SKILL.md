---
name: skill-planner
description: Use when starting any user task before deciding which available skills, plugins, or MCP tools should guide the work.
---

# Skill Planner

启动时的轻量路由器：只决定“要不要用 skill，以及先后顺序”。输出后立即执行，不把路由变成讨论。

## 快速流程

1. 看用户真正要完成什么
2. 扫描已注入的 skills / plugins / MCP 工具
3. 只选择会改变执行方式的能力
4. 按执行顺序输出路由，但不列出 `skill-planner` 自身，然后继续任务

## 输出格式

需要 `skill-planner` 之外的专项能力：

`skill-planner` 是内部路由器：必须调用，但永远不作为“📋 任务路由”的条目展示；编号从第一个其它必要能力开始。

```
📋 任务路由
① skill-name → 本轮负责什么
② skill-name → 本轮负责什么
```

不需要 `skill-planner` 之外的专项能力：

```
📋 任务路由
无需专项 skill，直接执行
```

## 核心规则

- **少列**：只列必要项；“可能有用”不算必要。
- **隐藏自身**：`skill-planner` 只负责生成路由，不列入用户可见路由；即使用户点名或任务对象是 `skill-planner`，也只列其它真正执行任务的能力。
- **必列**：除 `skill-planner` 自身外，用户点名的 skill、任务硬门槛、文件类型/领域强相关工具。
- **顺序**：外层流程先列，专业能力居中，验证/清理类放最后。
- **不等确认**：路由后直接开始；用户可随时纠正。
- **不展开解释**：必要项全部列出，每项一行；不为凑完整而补无关项。
- **不维护关键词表**：依靠当前可用能力和任务目标推理。

## 边界判断

- **无需专项 skill**：单步读文件、搜代码、查命令、解释概念。
- **需要专项 skill**：多步骤产物、代码修改、调试、评审、论文/PPT/表格/文档、联网核验、浏览器操作。
- **需求不清**：若目标或验收标准影响路线，先问一个具体问题；否则做合理假设并继续。

## 原则

路由是开工前的导航，不是计划书。准确优先，简短其次；能不列就不列，关键项不能漏。路由展示的是接下来要被调度的专项能力，不展示生成路由这一步本身。
