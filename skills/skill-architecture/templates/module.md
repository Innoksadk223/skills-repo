# {模块名}

> 模板，替换 `{...}` 占位符后删除此行。五段为推荐结构，非强制。

## 状态检查

检查 `state/{MODULE_ID}_{output_name}.{ext}` 是否存在。
- 存在 -> 读取并跳过本模块
- 不存在 -> 执行以下逻辑

## 输入

| 字段 | 类型 | 来源 | 说明 |
|------|------|------|------|
| `{field_1}` | `{type}` | `state/{前置模块}_output.json` | {说明} |
| `{field_2}` | `{type}` | 用户输入 | {说明} |

## 执行

1. {步骤 1}
2. {步骤 2}
3. {步骤 3}
（不超过 7 步）

## 输出

- **文件**: `state/{MODULE_ID}_{output_name}.{ext}`
- **格式**: {JSON / Markdown / 纯文本}
- **结构**:
```json
{ "key": "value" }
```

## 回滚

失败时：删除 `state/{MODULE_ID}_*`，重新运行本模块。前置模块输出不受影响。
